$(function() {
    $('.tournament-pool-fighters-sortable').sortable({
      connectWith: '.tournament-pool-fighters-sortable'
    });
    $('.tournament-pool-fighters-sortable').disableSelection();
});